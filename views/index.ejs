<%- include('partials/header') %>
<body>
    <%- include('partials/navbar') %>
    
    <div class="container">

        <h1>Asks</h1>
        <a href="/ask" class="btn btn-primary">To ask</a><hr>

    <% asks.forEach(ask => {   %> <br>

        <div class="card">
            <div class="card-body">
                <h3>
                     <%= ask.title %><br>
                </h3>
            </div>
            <div class="card-footer">
                <button class="btn btn-primary">Responder</button>
            </div>
        </div>
    <% }) %>

    </div>
</body>
<%- include('partials/footer') %>